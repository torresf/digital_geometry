PROJECT(tp1)
#Required in DGtal
CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
FIND_PACKAGE(DGtal REQUIRED)
# FIND_PACKAGE(Cairo)
INCLUDE_DIRECTORIES(${DGTAL_INCLUDE_DIRS})
LINK_DIRECTORIES(${DGTAL_LIBRARY_DIRS})
ADD_EXECUTABLE(tp1 main)
TARGET_LINK_LIBRARIES(tp1 ${DGTAL_LIBRARIES})
